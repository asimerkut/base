version: '2'
services:
    base-postgresql:
        image: postgres:10.4
        # volumes:
        #     - ~/volumes/jhipster/base/postgresql/:/var/lib/postgresql/data/
        environment:
            - POSTGRES_USER=base
            - POSTGRES_PASSWORD=
        ports:
            - 5432:5432
